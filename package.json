{
  "name": "fizzBuzz",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "babel src -d lib",
    "start": "yarn build && node lib/main.js"
  },
  "dependencies": {
    "@babel/cli": "^7.5.5",
    "jest": "^24.8.0"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5"
  }
}
